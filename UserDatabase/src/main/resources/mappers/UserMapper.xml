<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.parsons.mapper.UserMapper">
    <!-- SQL statements go here -->
    <select id="selectAll" resultType="com.parsons.pojo.User">
        SELECT * FROM user_data
    </select>
    <insert id="insert" parameterType="com.parsons.pojo.User">
        INSERT INTO user_data (ip_address, correctness, topic_category, duration, context)
        VALUES (#{ipAddress}, #{correctness}, #{topicCategory}, #{duration}, #{context})
    </insert>
</mapper>
